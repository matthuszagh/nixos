;;; gnus-layer.el --- Summary -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(layer-def gnus
  :setup
  (setq gnus-init-file (concat user-emacs-directory ".gnus.el")))

;;; gnus-layer.el ends here
