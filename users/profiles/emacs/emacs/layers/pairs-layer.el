;;; pairs-layer.el --- Summary -*- lexical-binding: t; -*-

;;; Commentary:

;;; Code:

(layer-def pairs
  :setup
  (use-package tab-jump-out
    :config
    (tab-jump-out-mode)))

;;; pairs-layer.el ends here
